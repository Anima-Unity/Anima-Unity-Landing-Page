import VetDirectoryTemplate from '@/app/components/templates/VetDirectoryTemplate'


// Main component
const VetDirectoryPage: React.FC = () => {
  
  return (
    <VetDirectoryTemplate/>
  );
};

export default VetDirectoryPage;